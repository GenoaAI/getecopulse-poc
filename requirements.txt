# GetEcoPulse — Python dependencies
# Vercel @vercel/python reads this file from the repo root.
# Pinned versions for reproducible builds.

fastapi==0.115.12
python-multipart==0.0.20  # required by FastAPI for UploadFile / Form fields
uvicorn==0.34.2
pydantic==2.13.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
PyYAML==6.0.2
requests==2.32.3
httpx==0.28.1
anyio==4.9.0

# AI — Gemini Vision + Search grounding via google-genai direct SDK
google-genai==2.5.0

# Supabase — storage + persistence (optional — no-op if env vars absent)
supabase==2.30.0

# Stripe — payment integration
stripe==12.1.0
