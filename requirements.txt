# GetEcoPulse — Python dependencies
# Vercel @vercel/python reads this file from the repo root.
# Pinned versions for reproducible builds.

fastapi==0.115.12
python-multipart==0.0.20  # required by FastAPI for UploadFile / Form fields
uvicorn==0.34.2
pydantic==2.13.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
PyYAML==6.0.2
requests==2.32.3
httpx==0.27.2
anyio==4.9.0

# AI — Gemini Vision via LiteLLM (no pandas/numpy = bundle < 150 MB)
litellm==1.59.10
# Gemini direct SDK — used for Google Search grounding (plausibility check)
google-genai==2.5.0
openai==1.60.2
tiktoken==0.8.0
regex==2026.5.9

# Supabase — storage + persistence (optional — no-op if env vars absent)
supabase==2.30.0

# Async HTTP (required by aiohttp + litellm internals)
aiohttp==3.11.11
aiohappyeyeballs==2.6.2
